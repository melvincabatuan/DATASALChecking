package ph.edu.dlsu.datasal.ocampo.mygraph;
import java.util.*;

/**
 * Unweighted directed graph. <br> <br>
 * In an unweighted digraph, the edges are unweighted but they are directed.
 * That is, the edges had a start node and end node. Edge connections are one
 * sided.
 * @param <E> The data type carried by its nodes.
 */
public class MyUnweightedDigraph<E> {
    
}
